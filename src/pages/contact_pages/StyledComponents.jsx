import { styled as muiStyled } from '@mui/material'
import { StyledButton, StyledInput } from '../../StyledComponents'

export const StyledContactInput = muiStyled(StyledInput)`
  width: 85%;
`
export const StyledContactButton = muiStyled(StyledButton)`
  width: 85%;
  border-radius: 25px;
  font-size: 16px;
`
